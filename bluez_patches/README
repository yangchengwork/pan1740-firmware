How to apply the Dialog patch on a bluez release
================================================
The Dialog patch adds support for Dialog's BTLE chips in bluez.
Apply the patch like below:

> cd /path/to/bluez_sources/
> patch -p1 -i /path/to/bluez-5.XX_dialog.patch

Build bluez as usually.

